import { BsFontDirective } from './bs-font.directive';

describe('BsFontDirective', () => {
  it('should create an instance', () => {
    const directive = new BsFontDirective();
    expect(directive).toBeTruthy();
  });
});

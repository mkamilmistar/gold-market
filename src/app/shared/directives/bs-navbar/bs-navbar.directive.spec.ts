import { BsNavbarDirective } from './bs-navbar.directive';

describe('BsNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new BsNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
